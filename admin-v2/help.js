const API_ORIGINS=['https://api.bunyan-sudan.org','https://bunyan-api-qhkf.onrender.com'];const tokenKey='bunyan_v2_token',originKey='bunyan_v2_origin',$=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),statusLabels={new:'جديد',review:'قيد المراجعة',approved:'معتمد',rejected:'مرفوض',completed:'مكتمل'};let rows=[];async function api(path,options={}){const token=sessionStorage.getItem(tokenKey)||'',preferred=sessionStorage.getItem(originKey)||API_ORIGINS[0],headers={...(options.headers||{})};if(token)headers.Authorization=`Bearer ${token}`;if(options.body){headers['Content-Type']='application/json';options={...options,body:typeof options.body==='string'?options.body:JSON.stringify(options.body)}}let lastError;for(const origin of [preferred,...API_ORIGINS.filter(x=>x!==preferred)]){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),90000);try{const response=await fetch(origin+path,{...options,headers,cache:'no-store',signal:controller.signal}),text=await response.text();let data={};try{data=text?JSON.parse(text):{}}catch{data={error:text}}if(!response.ok){const error=new Error(data.error||`فشل الطلب (${response.status})`);error.status=response.status;throw error}sessionStorage.setItem(originKey,origin);return data}catch(error){lastError=error;if(error.status&&error.status<500)break}finally{clearTimeout(timer)}}throw lastError||new Error('تعذر الاتصال بالخادم')}const date=value=>value?new Date(value).toLocaleString('ar-SD',{dateStyle:'medium',timeStyle:'short'}):'—',cleanPhone=value=>String(value||'').replace(/[^+\d]/g,''),field=(label,value,wide=false)=>`<div class="help-field ${wide?'wide':''}"><span>${esc(label)}</span><strong>${esc(value||'—')}</strong></div>`;function render(filter='all'){const list=filter==='all'?rows:rows.filter(row=>(row.status||'new')===filter);$('#content').innerHTML=`<section class="help-workspace"><div class="section-head"><div><h3>طلبات المساعدة</h3><p>راجع تفاصيل الحالة، اتصل بصاحبها، ثم حدّث مسار المعالجة.</p></div><span>${list.length} من ${rows.length}</span></div><div class="help-filters" role="group" aria-label="تصفية طلبات المساعدة">${[['all','الكل'],['new','جديد'],['review','قيد المراجعة'],['approved','معتمد'],['rejected','مرفوض'],['completed','مكتمل']].map(([value,label])=>`<button data-help-filter="${value}" class="${filter===value?'active':''}">${label}<small>${value==='all'?rows.length:rows.filter(x=>(x.status||'new')===value).length}</small></button>`).join('')}</div>${list.length?`<div class="help-list">${list.map((row,index)=>{const phone=cleanPhone(row.phone),current=row.status||'new';return `<article class="help-card" data-help-id="${esc(row.id)}"><header><div><span class="tracking">${esc(row.tracking_number||`طلب ${index+1}`)}</span><h4>${esc(row.full_name||row.name||'بدون اسم')}</h4></div><span class="status status-${esc(current)}">${esc(statusLabels[current]||current)}</span></header><div class="help-grid">${field('الهاتف',row.phone)}${field('نوع الحالة',row.case_type)}${field('الموقع',row.location)}${field('تاريخ الإرسال',date(row.created_at))}${field('وصف الحالة',row.description||row.details||row.message,true)}</div><footer><div class="help-contact">${phone?`<a class="ghost compact" href="tel:${esc(phone)}">اتصال</a><a class="ghost compact" href="https://wa.me/${esc(phone.replace(/^\+/,''))}" target="_blank" rel="noopener">واتساب</a>`:'<span>لا يوجد رقم اتصال</span>'}</div><div class="help-status-control"><select aria-label="حالة الطلب">${Object.entries(statusLabels).map(([value,label])=>`<option value="${value}" ${current===value?'selected':''}>${label}</option>`).join('')}</select><button class="primary compact" data-help-save>حفظ الحالة</button><span class="inline-message" role="status"></span></div></footer></article>`}).join('')}</div>`:'<div class="empty">لا توجد طلبات بهذه الحالة.</div>'}</section>`}async function openHelp(){$('#pageTitle').textContent='طلبات المساعدة';document.querySelectorAll('#adminNav [data-view]').forEach(button=>button.classList.toggle('active',button.dataset.view==='help'));$('#sidebar').classList.remove('open');$('#backdrop').classList.remove('show');$('#backdrop').hidden=true;$('#content').innerHTML='<div class="loading">جاري تحميل طلبات المساعدة...</div>';try{const data=await api('/api/help/requests');rows=Array.isArray(data)?data:[];$('#helpBadge').textContent=rows.filter(x=>(x.status||'new')==='new').length;render()}catch(error){if(error.status===401){sessionStorage.removeItem(tokenKey);location.reload();return}$('#content').innerHTML=`<div class="error">${esc(error.message)}</div><button class="ghost" data-help-retry>إعادة المحاولة</button>`}}document.addEventListener('click',async event=>{const nav=event.target.closest('#adminNav [data-view="help"]');if(nav){event.preventDefault();event.stopImmediatePropagation();await openHelp();return}const filter=event.target.closest('[data-help-filter]');if(filter){render(filter.dataset.helpFilter);return}if(event.target.closest('[data-help-retry]')){await openHelp();return}const save=event.target.closest('[data-help-save]');if(!save)return;const card=save.closest('.help-card'),message=card.querySelector('.inline-message'),status=card.querySelector('select').value;try{save.disabled=true;message.textContent='جاري الحفظ...';await api(`/api/help/requests/${card.dataset.helpId}`,{method:'PATCH',body:{status}});message.textContent='تم الحفظ بنجاح';await openHelp()}catch(error){message.textContent=error.message;save.disabled=false}},true);