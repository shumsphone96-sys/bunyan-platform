(()=>{
  'use strict';
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  const origins=window.BUNYAN_API_ORIGINS||[window.BUNYAN_API_ORIGIN||'https://api.bunyan-sudan.org','https://bunyan-api-qhkf.onrender.com'];
  const token=()=>sessionStorage.getItem('bunyan_token')||'';
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  let base=sessionStorage.getItem('bunyan_api_origin')||origins[0],rows=[];
  async function api(path,options={}){const headers={...(options.headers||{})};if(token())headers.Authorization=`Bearer ${token()}`;if(options.body&&!(options.body instanceof FormData)){headers['Content-Type']='application/json';if(typeof options.body==='object')options.body=JSON.stringify(options.body)}let last;for(const origin of [base,...origins.filter(x=>x!==base)]){try{const r=await fetch(origin+path,{...options,headers,cache:'no-store'}),text=await r.text();let data={};try{data=text?JSON.parse(text):{}}catch{data={message:text}}if(!r.ok){const e=new Error(data.error||data.message||`فشل الطلب (${r.status})`);e.status=r.status;throw e}base=origin;sessionStorage.setItem('bunyan_api_origin',origin);return data}catch(e){last=e;if(e.status&&e.status>=400&&e.status<500&&![401,403].includes(e.status))throw e}}throw last||new Error('تعذر الاتصال بالخادم')}
  const abs=u=>!u?'':/^https?:/i.test(u)?u:base+u;
  const status={planning:'قيد التخطيط',active:'نشط',paused:'متوقف',completed:'مكتمل',draft:'مسودة'};
  const card=p=>{const progress=Math.max(0,Math.min(100,Number(p.progress||0))),cover=abs(p.cover_image_url);return `<article class="pv4-card" data-search="${esc(`${p.name||''} ${p.location||''} ${p.status||''}`.toLowerCase())}" data-public="${p.is_public?'1':'0'}" data-status="${esc(p.status||'')}"><div class="pv4-cover">${cover?`<img src="${esc(cover)}" alt="${esc(p.name)}">`:'لا توجد صورة'}</div><div class="pv4-body"><div class="pv4-top"><span class="pv4-badge">${esc(status[p.status]||p.status||'جديد')}</span><strong class="${p.is_public?'pv4-live':'pv4-draft'}">${p.is_public?'منشور':'مخفي'}</strong></div><h4>${esc(p.name||'مشروع بلا اسم')}</h4><p>${esc(p.summary||'لا يوجد ملخص.')}</p><div class="pv4-progress"><i style="width:${progress}%"></i></div><div class="pv4-meta"><span>${progress}% إنجاز</span><span>${Number(p.beneficiaries_target||0).toLocaleString('en-GB')} مستفيد</span></div><div class="pv4-meta"><span>${Number(p.budget||0).toLocaleString('en-GB')} ${esc(p.currency||'SDG')}</span><span>${esc(p.location||'الموقع غير محدد')}</span></div><div class="pv4-actions"><button data-preview="${p.id}">معاينة</button><button data-edit="${p.id}">تعديل</button><button data-toggle="${p.id}">${p.is_public?'إخفاء':'نشر'}</button><button class="danger" data-delete="${p.id}">حذف</button></div></div></article>`};
  function bind(){$$('[data-preview]').forEach(b=>b.onclick=()=>preview(rows.find(x=>String(x.id)===b.dataset.preview)));$$('[data-edit]').forEach(b=>b.onclick=()=>editor(rows.find(x=>String(x.id)===b.dataset.edit)));$$('[data-toggle]').forEach(b=>b.onclick=()=>toggle(rows.find(x=>String(x.id)===b.dataset.toggle)));$$('[data-delete]').forEach(b=>b.onclick=()=>remove(b.dataset.delete));}
  async function render(){const content=$('#dashContent'),title=$('#dashTitle');if(!content)return;if(title)title.textContent='إدارة المشروعات';content.innerHTML='<div class="loading">جاري تحميل المشروعات...</div>';try{rows=await api('/api/projects');content.innerHTML=`<section class="pv4-head"><div><span class="tag dark">المرحلة الرابعة</span><h3>مركز إدارة المشروعات</h3><p>إنشاء، تعديل، نشر ومتابعة كل مشروع من مكان واحد.</p></div><button id="pv4Add" class="primary">+ مشروع جديد</button></section><div class="pv4-tools"><input id="pv4Search" placeholder="ابحث بالاسم أو الموقع"><select id="pv4Filter"><option value="all">كل المشروعات</option><option value="public">المنشورة</option><option value="hidden">المخفية</option><option value="active">النشطة</option><option value="completed">المكتملة</option></select><span class="count-badge">${rows.length} مشروع</span></div><div id="pv4Grid" class="pv4-grid">${rows.length?rows.map(card).join(''):'<div class="pv4-empty">لا توجد مشروعات بعد.</div>'}</div>`;$('#pv4Add').onclick=()=>editor();$('#pv4Search').oninput=filter;$('#pv4Filter').onchange=filter;bind()}catch(e){content.innerHTML=`<div class="error-msg">${esc(e.message)}</div>`}}
  function filter(){const q=$('#pv4Search')?.value.trim().toLowerCase()||'',f=$('#pv4Filter')?.value||'all';$$('.pv4-card').forEach(c=>{const okQ=!q||c.dataset.search.includes(q);const okF=f==='all'||(f==='public'&&c.dataset.public==='1')||(f==='hidden'&&c.dataset.public==='0')||c.dataset.status===f;c.hidden=!(okQ&&okF)})}
  function editor(item={}){$('#pv4Modal')?.remove();const m=document.createElement('div');m.id='pv4Modal';m.className='pv4-modal';m.innerHTML=`<form class="pv4-form"><button type="button" class="pv4-close">×</button><h2>${item.id?'تعديل المشروع':'إنشاء مشروع جديد'}</h2><label>اسم المشروع<input name="name" value="${esc(item.name||'')}" minlength="3" maxlength="160" required></label><label>الملخص<textarea name="summary" rows="3" minlength="10" maxlength="1200" required>${esc(item.summary||'')}</textarea></label><label>الوصف الكامل<textarea name="description" rows="6" maxlength="6000">${esc(item.description||'')}</textarea></label><div class="pv4-two"><label>الموقع<input name="location" value="${esc(item.location||'')}"></label><label>الحالة<select name="status"><option value="planning">قيد التخطيط</option><option value="active">نشط</option><option value="paused">متوقف</option><option value="completed">مكتمل</option></select></label></div><div class="pv4-two"><label>تاريخ البداية<input type="date" name="start_date" value="${item.start_date?String(item.start_date).slice(0,10):''}"></label><label>تاريخ النهاية<input type="date" name="end_date" value="${item.end_date?String(item.end_date).slice(0,10):''}"></label></div><div class="pv4-two"><label>الميزانية<input type="number" min="0" step="0.01" name="budget" value="${Number(item.budget||0)}"></label><label>العملة<select name="currency"><option>SDG</option><option>SAR</option><option>USD</option></select></label></div><div class="pv4-two"><label>المستفيدون<input type="number" min="0" name="beneficiaries_target" value="${Number(item.beneficiaries_target||0)}"></label><label>نسبة الإنجاز<input type="number" min="0" max="100" name="progress" value="${Number(item.progress||0)}"></label></div><label>رابط صورة الغلاف<input name="cover_image_url" value="${esc(item.cover_image_url||'')}" placeholder="https://..."></label><label class="pv4-check"><input type="checkbox" name="is_public" ${item.is_public?'checked':''}> نشر المشروع في الموقع العام</label><div class="pv4-submit"><button type="button" class="pv4-cancel">إلغاء</button><button class="primary" type="submit">حفظ المشروع</button></div><small class="pv4-msg"></small></form>`;document.body.appendChild(m);m.querySelector('[name=status]').value=item.status||'planning';m.querySelector('[name=currency]').value=item.currency||'SDG';const close=()=>m.remove();m.querySelector('.pv4-close').onclick=close;m.querySelector('.pv4-cancel').onclick=close;m.onclick=e=>{if(e.target===m)close()};m.querySelector('form').onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,msg=f.querySelector('.pv4-msg'),btn=f.querySelector('[type=submit]'),fd=new FormData(f);const body={name:fd.get('name'),summary:fd.get('summary'),description:fd.get('description')||null,location:fd.get('location')||null,status:fd.get('status'),start_date:fd.get('start_date')||null,end_date:fd.get('end_date')||null,budget:Number(fd.get('budget')||0),currency:fd.get('currency'),beneficiaries_target:Number(fd.get('beneficiaries_target')||0),progress:Number(fd.get('progress')||0),cover_image_url:fd.get('cover_image_url')||null,is_public:f.elements.is_public.checked};try{btn.disabled=true;msg.textContent='جاري الحفظ...';await api(item.id?`/api/projects/${item.id}`:'/api/projects',{method:item.id?'PATCH':'POST',body});msg.textContent='تم الحفظ بنجاح.';setTimeout(()=>{close();render();window.loadPublic?.()},450)}catch(x){msg.textContent=x.message}finally{btn.disabled=false}}}
  function preview(p){if(!p)return;const m=document.createElement('div');m.className='pv4-modal';const cover=abs(p.cover_image_url);m.innerHTML=`<article class="pv4-preview"><button class="pv4-close">×</button>${cover?`<img class="pv4-preview-cover" src="${esc(cover)}">`:''}<span class="pv4-badge">${esc(status[p.status]||p.status)}</span><h2>${esc(p.name)}</h2><p>${esc(p.description||p.summary||'')}</p><div class="pv4-meta"><span>${esc(p.location||'')}</span><span>${Number(p.progress||0)}% إنجاز</span><span>${Number(p.budget||0).toLocaleString('en-GB')} ${esc(p.currency||'')}</span></div></article>`;document.body.appendChild(m);m.querySelector('.pv4-close').onclick=()=>m.remove();m.onclick=e=>{if(e.target===m)m.remove()}}
  async function toggle(p){if(!p)return;try{await api(`/api/projects/${p.id}`,{method:'PATCH',body:{is_public:!p.is_public}});await render();window.loadPublic?.()}catch(e){alert(e.message)}}
  async function remove(id){if(!confirm('حذف المشروع نهائياً؟'))return;try{await api(`/api/projects/${id}`,{method:'DELETE'});await render();window.loadPublic?.()}catch(e){alert(e.message)}}
  function install(){const b=$('[data-view="projects"]');if(!b)return;b.addEventListener('click',e=>{e.preventDefault();render()},true);window.openProjectCenter=render;window.openGlobalProjectEditor=editor}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();